# Assets

Drop the following image files here. Names must match exactly (they're referenced by `app.json`).

| File | Size | Description |
|---|---|---|
| `icon.png` | 1024×1024 | App icon — bold "P" mark with the primary gradient `#FF4DA6 → #7B3FF2 → #00D1FF` on a `#0A0A12` background. |
| `splash.png` | 1242×2436 | Splash — centered Partybond wordmark + logo with soft neon glow on `#0A0A12`. |
| `adaptive-icon.png` | 432×432 | Android adaptive foreground — only the "P" mark with the brand gradient, transparent background. |
| `notification-icon.png` | 96×96 | Monochrome white silhouette of the "P" on transparent — Android notifications will tint it with `#7B3FF2`. |

Until you add real files, Expo will warn at start-up but the app will still run.
